# Copie para deploy.env e ajuste. deploy.env NÃO vai para o git.
# A autenticação é por chave SSH: instale sua chave pública no servidor com
#   ssh-copy-id USUARIO@SERVIDOR
DEPLOY_HOST=servidor.exemplo.com
DEPLOY_USER=usuario
DEPLOY_PATH=/home/usuario/public_html
DEPLOY_URL=https://exemplo.com.br
